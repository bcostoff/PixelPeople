{
  "name": "pixel-people",
  "version": "1.0.0",
  "description": "Fun guess em game built in react.",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "deploy": "node server/index.js && cd client && npm install && npm run build"
  },
  "engines": {
    "node": "16.14.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "express": "^4.17.3",
    "mysql": "^2.18.1"
  }
}
